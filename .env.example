# ── Supabase ──────────────────────────────────────────────────────────────────
# Public keys (safe for the browser — VITE_ prefix exposes them client-side)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>

# ── Authentication ───────────────────────────────────────────────────────────
# Auth is handled via Supabase Auth (email/password).
# When VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are set, the dashboard
# requires login. If they are unset, auth is bypassed so the dashboard works
# locally without a Supabase project.
#
# Pre-created accounts (create in Supabase Auth dashboard):
#   team@guardianlife.com   — shared team account (role: team)
#   <your-email>            — admin account    (role: admin)
#
# Roles are stored in the public.profiles table (id FK → auth.users, role text).
